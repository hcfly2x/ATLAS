# Epic 01 — Foundation mínima

> Trilha 1 — Fase 1. Executar somente após autorização explícita.

## Tarefas
- monorepo pnpm + Turborepo + TypeScript estrito;
- apps `coordinator` e `worker`;
- packages mínimos definidos em `docs/implementation-plan.md`;
- PostgreSQL para desenvolvimento do coordinator; sem Redis;
- nenhum Docker ou banco no worker;
- lint, formatação, testes, build e CI;
- logs estruturados com correlation ID.

## Aceite
Pipeline verde e todos os apps/packages compilando.
