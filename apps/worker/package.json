{
  "name": "@atlas/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint . --max-warnings 0",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@atlas/codex-adapter": "workspace:*",
    "@atlas/git-adapter": "workspace:*",
    "@atlas/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
